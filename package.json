{
  "scripts": {
    "docs:dev": "rm -rf docs && ln -s ../docs docs && vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "deploy": "gh-pages -d .vitepress/dist"
  },
  "dependencies": {
    "colorette": "^2.0.20",
    "gh-pages": "^6.1.1",
    "vitepress": "^1.0.1",
    "vitepress-auto-navigation": "^0.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.12.7"
  }
}
